# G9 multi-AI baton (read this FIRST, before anything else)

To resume ANY G9 work (including when another AI's usage limit was hit), read ONLY:

`C:\Users\exoni\Desktop\Gmail Sort\03_Obsidian_Vault\G9_RESUME_NOW.md`

That file is the live baton shared by ALL AIs: the holistic picture, a last-hours DONE
ledger, live claims, and the exact next step. Follow its protocol exactly: CLAIM your item
in the file before starting, mark it DONE there the moment it is verified, and rewrite the
file as you go (not at session end). Never redo a DONE item; a returning AI reconciles
(checks what others did, fixes imperfections) instead of redoing.

Do NOT re-read the codebase or the vault to "catch up" - the baton IS the catch-up.
Read other files only when your current step names them. Obey the baton's HARD RULES
(user runs git push; never delete restore points; no em dashes; sharp-90 UI).
